# Douyin Monitor UI

React + TypeScript + Tailwind dashboard for the local Douyin Hot Monitor Studio.

## Run

```bash
npm install
cp .env.example .env.local
npm run dev
```

Open `http://127.0.0.1:5174/`.

The local control API runs at `http://127.0.0.1:8787/`.
